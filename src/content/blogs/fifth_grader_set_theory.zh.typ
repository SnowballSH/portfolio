#metadata(
  (
    title: "解一道简短却不简单的中国奥数入门题",
    description: "为我五年级邻居困难的数学作业提供一份严谨的解答。",
    date: "2026-05-12",
    tags: ("math", "algebra"),
    // hidden: true,
  ),
)<frontmatter>

#set text(
  font: ("Kaiti SC", "KaiTi", "Libertinus Serif"),
  fallback: true,
)
#set page(height: auto, margin: 0.7em)
#set par(first-line-indent: (amount: 1em, all: true))
#show link: set text(fill: rgb("#3eff54"))
#show link: underline

#set quote(block: true)
#show quote: set align(center)
#show heading: set block(below: 1em)

（注：由于我有点懒，此中文版本绝大部分是用 Google Gemini 3.1 Pro 英译中的。若有误，敬请谅解。）

= 引言

显然，我上海的新邻居是个五年级的天才，他现在已经在学习高中课程了。因此，说他正在准备数学奥赛也就不足为奇了。今天，他给我看了一道他奥数班的作业题，把我给难住了。

题目的主题是“集合”。我心想，作为一个大学生，五年级学生的集合论题目总不至于太难吧？

他指着作业上的第3题，告诉我这是他第一道解不出来的题。

盯着这道题看了十分钟后，我没能解出这道题的第二问。为了在尴尬之后挽回一点颜面，我把我的解答写在这里。因为这道题有很多边界情况和容易出错的地方，我将写下一份*极其严谨的解答*。

哦对了，这道题也让我想起为什么我当年*没有*在国内搞奥数。我不确定这道题的出处。如果你找到了这道题的出处，请告诉我。

1%的集合，99%的代数。

= 题目

给定 $a in RR$，定义 $f(x) = a x^2 - 1$。定义集合 $A = {x in RR | f(x) = x}$ 和 $B = {x in RR | f(f(x)) = x}$。

(a) 证明 $A subset.eq B$。

(b) 求所有满足 $A = B != emptyset$ 的 $a$ 的可能取值，并给出证明。

= 备注

第(a)问很简单——它仅仅是测试选手是否知道什么是子集。

第(b)问……不知道为什么这会出现在集合的作业里。我唯一的瓶颈是对代数的观察。

= 我的解答

(a) _证：_ 设 $x in A$。则 $f(x) = x$，所以 $f(f(x)) = f(x) = x$，这意味着 $x in B$。因此，$A subset.eq B$。 $qed$

(b) 我断言所有可能的 $a$ 的取值集合为 $[-1/4, 3/4]$。

_证：_ 我们将证明分为两个断言。

*断言 1:* $A != emptyset$ 当且仅当 $a >= -1/4$。

_断言1的证明：_

$(=>)$ #text(red)[首先注意到如果 $a = 0$]，该断言显然成立（因为 $0 >= -1/4$）。

#text(yellow)[对于 $a != 0$ 的情况]，如果 $A != emptyset$，则 $f(x)-x=a x^2-x-1$ 有实根。因此，$Delta = (-1)^2 - 4(a)(-1) >= 0$，所以 $a >= -1/4$。

$(arrow.l.double)$ #text(red)[首先注意到如果 $a = 0$]，则 $f(x) = -1$，所以 $f(-1)=-1$ 是一个不动点，因此 $-1 in A$，即 $A != emptyset$。

#text(yellow)[对于 $a != 0$ 的情况]，如果 $a >= -1/4$，则 $Delta>=0$，所以 $f(x)-x$ 至少有一个实根，这意味着存在某个 $x in RR$ 使得 $x in A$。 $qed$

*断言 2:* $A = B$ 当且仅当 $a <= 3/4$。

_断言2的证明：_ 我们有 $-1 = f(x) - a x^2$。因此，
$ f(f(x))-x & = a (f(x))^2 - 1 - x \
          & = a (f(x))^2 + f(x) - a x^2 - x \
          & = a (f(x)-x)(f(x)+x) + (f(x) - x) \
          & = (a(f(x)+x) + 1)(f(x)-x) $ 对所有 $x in RR$ 成立。

为方便起见，记 $g(x) = a^2 x^2 + a x + (1-a)$。则 $f(f(x))-x=g(x)(f(x)-x)$。

#text(red)[首先注意到如果 $a = 0$]，则我们得到 $f(f(x)) - x = (0+1)(f(x)-x) = f(x)-x$ 对所有 $x in RR$ 成立，因此 $A = B$。

接下来，我们将针对 #text(yellow)[$a != 0$ 的情况] 证明断言2。

#text(yellow)[$(=>)$] 因为 $A = B$，必然有 $B subset.eq A$。因此 $f(f(x))-x$ 的任何根也是 $f(x)-x$ 的根。所以，要么 $g(x)$ 没有实根，要么 $g(x)$ 的根是 $f(x)-x$ 的根的子集。

情况 1：$g(x)$ 没有实根。那么，$Delta = a^2 - 4 a^2 (1-a) < 0$，所以 $a^2 (4a-3) < 0$，又 #text(yellow)[因为 $a != 0$]，这得出 $a < 3/4$。

情况 2：$g(x)$ 至少有一个实根，并且 $g(x)$ 的所有根也是 $f(x)-x$ 的根。

在这种情况下，$forall r in RR$，$g(r)=0 ==> f(r)-r=0$。

设 $r$ 为 $g(x)$ 的一个根。那么，$a^2 r^2 + a r + (1 - a) = 0$ 且 $a r^2 - r - 1 = 0$。代入可得 $a(r+1) + a r + (1-a) = 0$，即 $2a r + 1 = 0$，且 #text(yellow)[因为 $a != 0$]，这得出 $r = -1/(2a)$。因此，$g(x)$ 必须恰好有一个实根，且必须是 $-1/(2a)$。在这种情况下，$Delta = 0$，所以 $a^2 (4a-3) = 0$，又 #text(yellow)[因为 $a != 0$]，可得 $a = 3/4$。

至此所有情况讨论完毕。在所有情况下，皆有 $a <= 3/4$。

#text(yellow)[$(arrow.l.double)$] 同上，如果 $a < 3/4$，则 $g(x)$ 没有实根，所以 $f(f(x))-x=0 ==> f(x)-x = 0$，即 $B subset.eq A$。

如果 $a = 3/4$，则 $g(x)$ 恰好有一个根 $-1/(2a) = -2/3$，而在这种情况下，$f(-2/3)-(-2/3)=(3/4)(-2/3)^2 - 1 + 2/3 = 0$，所以 $f(f(x))-x=0 ==> f(x)-x = 0$，这意味着 $B subset.eq A$。

因此，如果 $a <= 3/4$ 且 #text(yellow)[$a != 0$]，则 $B subset.eq A$。此外，在第(a)问中，我们得出 $A subset.eq B$。因此，我们有 $a <= 3/4 and a != 0 ==> A = B$。

因此，#text(yellow)[两个方向]均已证明，在 $a != 0$ 的情况下，我们有 $a <= 3/4$ 当且仅当 $A = B$。

结合 #text(red)[$a=0$ 的情况]，我们证明了断言 2。 $qed$

由断言 1 和断言 2，$A = B$ 且 $A != emptyset$ 当且仅当 $a >= -1/4$ 且 $a <= 3/4$，所以 $a$ 的所有可能取值集合为 $[-1/4, 3/4]$。 $qed$

---
import Layout from "../../layouts/Layout.astro";
import { getCollection } from "astro:content";
import type { GetStaticPaths } from "astro";
import type { CollectionEntry } from "astro:content";

export const getStaticPaths: GetStaticPaths = async () => {
  const projects = await getCollection("projects");
  return projects.map((project) => ({
    params: { slug: project.slug },
    props: { project },
  }));
};

interface Props {
  project: CollectionEntry<"projects">;
}

const { project } = Astro.props as Props;
const { Content } = await project.render();
const {
  title,
  shortDescription,
  date,
  technologies,
  image,
  imageAlt,
  githubUrl,
  liveUrl,
  demoUrl,
  links,
  status,
  category,
} = project.data;

const formattedDate = new Intl.DateTimeFormat("en-US", {
  year: "numeric",
  month: "long",
  day: "numeric",
}).format(date);

const statusStyles = {
  completed: "bg-green-500/20 text-green-300 border-green-500/30",
  "in-progress": "bg-yellow-500/20 text-yellow-300 border-yellow-500/30",
  planned: "bg-blue-500/20 text-blue-300 border-blue-500/30",
};
---

<Layout
  title={`${title} - SnowballSH (Yinuo Huang)`}
  description={shortDescription}
>
  <div class="futuristic-bg"></div>
  <div class="geometric-overlay"></div>

  <div class="fixed inset-0 overflow-hidden pointer-events-none z-0">
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
    <div class="particle"></div>
  </div>

  <main class="relative z-10 min-h-screen">
    <div class="container mx-auto px-6 pt-8">
      <a
        href="/"
        class="inline-flex items-center text-primary-400 hover:text-primary-300 transition-colors mb-8"
      >
        ← Back to Portfolio
      </a>
    </div>

    <section class="py-12">
      <div class="container mx-auto px-6">
        <div class="max-w-4xl mx-auto">
          <div class="flex flex-wrap items-center gap-4 mb-6">
            <span class="text-gray-400">{formattedDate}</span>
            <span class="text-gray-500">•</span>
            <span class="capitalize text-gray-400">{category}</span>
            {
              status !== "completed" && (
                <>
                  <span class="text-gray-500">•</span>
                  <div
                    class={`inline-flex items-center px-3 py-1 rounded-full text-sm font-medium border ${statusStyles[status as keyof typeof statusStyles]}`}
                  >
                    {status === "in-progress" ? "In Progress" : "Planned"}
                  </div>
                </>
              )
            }
          </div>

          <h1
            class="text-5xl md:text-6xl font-display font-bold mb-6 text-white"
          >
            {title}
          </h1>

          <p class="text-xl text-dark-300 mb-8 leading-relaxed">
            {shortDescription}
          </p>

          <div class="flex flex-wrap gap-3 mb-8">
            {
              technologies.map((tech: string) => (
                <span class="px-4 py-2 bg-primary-500/20 text-primary-300 rounded-full font-medium">
                  {tech}
                </span>
              ))
            }
          </div>

          <div class="flex flex-wrap gap-4 mb-12">
            {
              liveUrl && (
                <a
                  href={liveUrl}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="glass-button px-6 py-3 font-semibold"
                >
                  View Live Site
                </a>
              )
            }

            {
              demoUrl && (
                <a
                  href={demoUrl}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="glass-button px-6 py-3 font-semibold"
                >
                  Try Demo
                </a>
              )
            }

            {
              githubUrl && (
                <a
                  href={githubUrl}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="glass-button px-6 py-3 font-semibold"
                >
                  View Source
                </a>
              )
            }

            {
              links &&
                links.map((link: any) => (
                  <a
                    href={link.url}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="glass-button px-6 py-3 font-semibold"
                  >
                    {link.label}
                  </a>
                ))
            }
          </div>
        </div>
      </div>
    </section>

<<<<<<< ours
    {
      image && (
        <section class="py-8">
          <div class="container mx-auto px-6">
            <div class="max-w-4xl mx-auto">
              <div class="glass-panel p-4">
                <img
                  src={image}
                  alt={imageAlt || `Screenshot of ${title}`}
                  class="w-full h-80 object-contain rounded-lg"
                  loading="lazy"
                />
              </div>
=======
    {image && (
      <section class="py-8">
        <div class="container mx-auto px-6">
          <div class="max-w-4xl mx-auto">
            <div class="glass-panel p-4">
              <img
                src={image}
                alt={imageAlt || `Screenshot of ${title}`}
                width="800"
                height="480"
                class="w-full h-80 object-contain rounded-lg"
                loading="eager"
              />
>>>>>>> theirs
            </div>
          </div>
        </section>
      )
    }

    <section class="py-12">
      <div class="container mx-auto px-6">
        <div class="max-w-4xl mx-auto">
          <article class="glass-panel prose-content" style="padding: 3rem;">
            <div class="markdown-content">
              <Content />
            </div>
          </article>
        </div>
      </div>
    </section>

    <section class="py-20">
      <div class="container mx-auto px-6">
        <h2
          class="text-3xl font-display font-bold text-center mb-12 text-white"
        >
          Other Projects
        </h2>
        <div class="text-center">
          <a
            href="/projects"
            class="glass-button px-8 py-4 text-lg font-semibold"
          >
            View All Projects
          </a>
        </div>
      </div>
    </section>
  </main>
</Layout>

<style>
  .prose-content {
    max-width: none !important;
  }

  .glass-panel .prose-content h1,
  .prose-content h1 {
    font-size: 2.25rem !important;
    font-weight: 700 !important;
    color: #ffffff !important;
    margin-bottom: 1.5rem !important;
    margin-top: 2rem !important;
    line-height: 1.2 !important;
    font-family: "Poppins", system-ui, sans-serif !important;
    display: block !important;
  }

  .glass-panel .prose-content h1:first-child,
  .prose-content h1:first-child {
    margin-top: 0 !important;
  }

  .glass-panel .prose-content h2,
  .prose-content h2 {
    font-size: 1.875rem !important;
    font-weight: 600 !important;
    color: #ffffff !important;
    margin-bottom: 1rem !important;
    margin-top: 2.5rem !important;
    line-height: 1.3 !important;
    font-family: "Poppins", system-ui, sans-serif !important;
    display: block !important;
  }

  .glass-panel .prose-content h3,
  .prose-content h3 {
    font-size: 1.5rem !important;
    font-weight: 600 !important;
    color: #ffffff !important;
    margin-bottom: 0.75rem !important;
    margin-top: 2rem !important;
    line-height: 1.4 !important;
    font-family: "Poppins", system-ui, sans-serif !important;
    display: block !important;
  }

  .glass-panel .prose-content h4,
  .prose-content h4 {
    font-size: 1.25rem !important;
    font-weight: 600 !important;
    color: #ffffff !important;
    margin-bottom: 0.5rem !important;
    margin-top: 1.5rem !important;
    line-height: 1.4 !important;
    font-family: "Poppins", system-ui, sans-serif !important;
    display: block !important;
  }

  .glass-panel .prose-content p,
  .prose-content p {
    color: #d1d5db !important;
    margin-bottom: 1.25rem !important;
    line-height: 1.75 !important;
    font-size: 1.125rem !important;
    display: block !important;
  }

  .glass-panel .prose-content ul,
  .glass-panel .prose-content ol,
  .prose-content ul,
  .prose-content ol {
    color: #d1d5db !important;
    margin-bottom: 1.25rem !important;
    padding-left: 1.5rem !important;
    line-height: 1.75 !important;
    display: block !important;
  }

  .glass-panel .prose-content li,
  .prose-content li {
    margin-bottom: 0.5rem !important;
    font-size: 1.125rem !important;
    color: #d1d5db !important;
    display: list-item !important;
  }

  .glass-panel .prose-content ul li,
  .prose-content ul li {
    list-style-type: disc !important;
  }

  .glass-panel .prose-content ol li,
  .prose-content ol li {
    list-style-type: decimal !important;
  }

  .glass-panel .prose-content strong,
  .glass-panel .prose-content b,
  .prose-content strong,
  .prose-content b {
    color: #ffffff !important;
    font-weight: 600 !important;
  }

  .glass-panel .prose-content em,
  .glass-panel .prose-content i,
  .prose-content em,
  .prose-content i {
    color: #e5e7eb !important;
    font-style: italic !important;
  }

  .glass-panel .prose-content a,
  .prose-content a {
    color: #00aeef !important;
    text-decoration: underline !important;
    text-underline-offset: 2px !important;
    transition: color 0.3s ease !important;
  }

  .glass-panel .prose-content a:hover,
  .prose-content a:hover {
    color: #38bdf8 !important;
  }

  .glass-panel .prose-content code,
  .prose-content code {
    color: #7dd3fc !important;
    background: rgba(0, 174, 239, 0.15) !important;
    padding: 0.25rem 0.5rem !important;
    border-radius: 0.375rem !important;
    font-size: 0.9em !important;
    font-family: "Courier New", monospace !important;
  }

  .glass-panel .prose-content pre,
  .prose-content pre {
    background: rgba(15, 23, 42, 0.8) !important;
    padding: 1.5rem !important;
    border-radius: 0.75rem !important;
    overflow-x: auto !important;
    margin: 1.5rem 0 !important;
    border: 1px solid rgba(255, 255, 255, 0.1) !important;
    display: block !important;
  }

  .glass-panel .prose-content pre code,
  .prose-content pre code {
    background: none !important;
    padding: 0 !important;
    color: #e2e8f0 !important;
    font-size: 0.875rem !important;
  }

  .glass-panel .prose-content blockquote,
  .prose-content blockquote {
    border-left: 4px solid #00aeef !important;
    padding-left: 1.5rem !important;
    margin: 1.5rem 0 !important;
    color: #cbd5e1 !important;
    font-style: italic !important;
    background: rgba(0, 174, 239, 0.05) !important;
    padding: 1rem 1.5rem !important;
    border-radius: 0.5rem !important;
    display: block !important;
  }

  .glass-panel .prose-content hr,
  .prose-content hr {
    border: none !important;
    height: 1px !important;
    background: rgba(255, 255, 255, 0.2) !important;
    margin: 2rem 0 !important;
    display: block !important;
  }

  .glass-panel .prose-content img,
  .prose-content img {
    border-radius: 0.75rem !important;
    margin: 1.5rem 0 !important;
    max-width: 100% !important;
    height: auto !important;
    display: block !important;
  }

  .glass-panel .prose-content table,
  .prose-content table {
    width: 100% !important;
    border-collapse: collapse !important;
    margin: 1.5rem 0 !important;
    display: table !important;
  }

  .glass-panel .prose-content th,
  .glass-panel .prose-content td,
  .prose-content th,
  .prose-content td {
    border: 1px solid rgba(255, 255, 255, 0.2) !important;
    padding: 0.75rem !important;
    text-align: left !important;
  }

  .glass-panel .prose-content th,
  .prose-content th {
    background: rgba(0, 174, 239, 0.1) !important;
    color: #ffffff !important;
    font-weight: 600 !important;
  }

  .glass-panel .prose-content td,
  .prose-content td {
    color: #d1d5db !important;
  }
</style>
